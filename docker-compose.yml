services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/.venv
      - /app/__pycache__
    restart: unless-stopped
