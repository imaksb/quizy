services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/.venv
      - /app/__pycache__
    restart: unless-stopped
